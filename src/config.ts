import { SetRootAtomsOptions } from './interfaces';

export const THEME_TOKEN = 'Qtheme';
export const THEME_COMMON_TOKEN = 'Qtheme-common';

export const DEFAULT_OPTIONS: SetRootAtomsOptions = {
  generateCSS: true,
  token: THEME_TOKEN,
  commonToken: THEME_COMMON_TOKEN,
};
